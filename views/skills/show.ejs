<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Developer Skill</title>
</head>
<body>
    <h3><%= skill.skill %></h3>
    <p>
        <%= skill.complete ? `GA has taught me a lot about ${skill.skill}!`: `I am still in the process of learning ${skill.skill}!` %>
    </p>
    <form action="/skills/<%= skill.id %>?_method=DELETE" method="POST">
        <button type="submit">DELETE</button>
    </form>
    <a href="/skills/<%= skill.id %>/edit">Edit Skill</a>
</body>
</html>